diff a/packages/socket/src/pushRequestUsing.js b/packages/socket/src/pushRequestUsing.js	(rejected hunks)
@@ -30 +30,2 @@ const setNotifierRequestStatusSending = (absintheSocket, notifier) =>
-const createRequestError = message => new Error(typeof message === "string" ? message : JSON.stringify(message));
+const createRequestError = message =>
+  new Error(typeof message === "string" ? message : JSON.stringify(message));
